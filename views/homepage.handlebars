<ol class="post-list">
    <li>
        <article class="post">
            <div class="title">
                <a href="{{post_url}}" target ="_blank" class="">{{title}}</a>
                <span>({{post_url}})</span>
            </div>
            <div>
                <a href="/post/{{id}}" class="">{{comments.length}} comment(s)</a>
            </div>
            
        </article>
    </li>
</ol>