<section>
  <h2>Mountain Resorts</h2>

  <form class="new-review-form">
    <div>
      <label for="review-title">Title</label>
      <input type="text" id="review-title" name="review-title" />
    </div>
    <div>
      <label for="review-text">Link</label>
      <input id="review-text" name="review-text" />
    </div>
    <button type="submit" class="btn">Create</button>
  </form>
</section>
{{!-- {{#if reviews.length}}
<section>
  <h2>Your Reviews</h2>
  <ol>
    {{#each reviews as |review|}}
    <li>
      {{> review-info review}}
      <a href="/dashboard/edit/{{review.id}}" class="edit-link">Edit post</a>
    </li>
    {{/each}}
  </ol>
</section>
{{/if}} --}}
<script src="/javascript/add-review.js"></script>